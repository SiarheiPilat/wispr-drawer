# Project Rules

## Documentation

Every feature, config option, and user-facing behavior must be documented in README.md. When adding or changing functionality, update the README as part of the same change.

## Settings

Every new feature with tunable parameters must be exposed in the Settings UI. Users should never have to edit config.json by hand to adjust behavior.
